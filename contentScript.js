//file is meant to interact with email page directly
function viewEmail() {
    const body = document.querySelector()
}

function senderinfo() {
    const senderElement = document.querySelector(`span[email]`);
    if(senderElement){
        return sender.getAttribte ('email');
    }
    return "Sender not found";
}

window.getSenderInfo = getSenderInfo;